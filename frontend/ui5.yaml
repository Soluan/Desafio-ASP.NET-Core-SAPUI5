specVersion: '3.0'
metadata:
  name: myapp-frontend
type: application
server:
  customMiddleware:
    - name: ui5-middleware-simpleproxy
      beforeMiddleware: serveResources
      configuration:
        mountPath: /users
        baseUri: "http://127.0.0.1:5000"
        strictSSL: false
